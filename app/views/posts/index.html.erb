<div class="section">
  <div class="post-wrapper"> 

    <h2> Stuff, I've written </h2>


    <div class= "post" >
      <hr>
      <% @posts.each do |post| %>
	      <h2><a href="artcile_show.html"> <%= link_to post.title, post %></a></h2>
                <p class="date"><%= post.created_at.strftime('%A, %B %d') %></p>

	        <hr>
      <% end %>
  </div>

      <%= will_paginate @posts %>

  </div>
</div>

